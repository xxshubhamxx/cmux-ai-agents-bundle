# Notify on build fail

Run any build command; if it exits non-zero, send a cmux notification with the last 5 lines of output.

## Run

```bash
chmod +x recipe.sh
./recipe.sh
```

See [`recipe.sh`](./recipe.sh) for the full script.
