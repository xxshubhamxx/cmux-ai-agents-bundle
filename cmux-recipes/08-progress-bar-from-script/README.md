# Sidebar progress bar from any script

Drive cmux's sidebar progress bar from a loop. Great for long batch jobs.

## Run

```bash
chmod +x recipe.sh
./recipe.sh
```

See [`recipe.sh`](./recipe.sh) for the full script.
