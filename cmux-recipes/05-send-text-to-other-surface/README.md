# Send a command to another surface

Send a shell command into a different surface in the same workspace without stealing focus.

## Run

```bash
chmod +x recipe.sh
./recipe.sh
```

See [`recipe.sh`](./recipe.sh) for the full script.
