# Detect cmux context in any script

Idiomatic 4-line check: am I inside cmux? Useful at the top of every hook script.

## Run

```bash
chmod +x recipe.sh
./recipe.sh
```

See [`recipe.sh`](./recipe.sh) for the full script.
