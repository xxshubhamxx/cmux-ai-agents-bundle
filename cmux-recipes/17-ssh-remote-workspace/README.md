# Open an SSH workspace to a remote VPS

One-liner: open a new workspace SSH'd into a remote box, without stealing focus.

## Run

```bash
chmod +x recipe.sh
./recipe.sh
```

See [`recipe.sh`](./recipe.sh) for the full script.
