# Tail a Vercel deploy with status pill

Tail `vercel logs --follow` in a helper pane and update a deploy pill in the sidebar.

## Run

```bash
chmod +x recipe.sh
./recipe.sh
```

See [`recipe.sh`](./recipe.sh) for the full script.
