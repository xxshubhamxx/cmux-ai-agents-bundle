# Sticky sidebar status pill

Pin a status pill to the workspace tab so the user sees state at a glance.

## Run

```bash
chmod +x recipe.sh
./recipe.sh
```

See [`recipe.sh`](./recipe.sh) for the full script.
