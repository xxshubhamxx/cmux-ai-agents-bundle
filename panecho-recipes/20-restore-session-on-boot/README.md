# Trigger session restore from login script

Add this to your shell rc so cmux restores yesterday's layout when you open a fresh session.

## Run

```bash
chmod +x recipe.sh
./recipe.sh
```

See [`recipe.sh`](./recipe.sh) for the full script.
