# Three agents on one PR

Spawn three terminal surfaces, each running a different reviewer (code/security/tests) against the same PR.

## Run

```bash
chmod +x recipe.sh
./recipe.sh
```

See [`recipe.sh`](./recipe.sh) for the full script.
