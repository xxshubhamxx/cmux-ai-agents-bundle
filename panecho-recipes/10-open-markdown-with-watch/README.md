# Open a markdown file in the live viewer

Open any .md file in cmux's markdown viewer; the panel auto-refreshes on every save.

## Run

```bash
chmod +x recipe.sh
./recipe.sh
```

See [`recipe.sh`](./recipe.sh) for the full script.
