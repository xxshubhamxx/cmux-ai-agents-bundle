# Screenshot a browser surface

Open a URL in a cmux browser surface, wait until loaded, and save a PNG screenshot.

## Run

```bash
chmod +x recipe.sh
./recipe.sh
```

See [`recipe.sh`](./recipe.sh) for the full script.
