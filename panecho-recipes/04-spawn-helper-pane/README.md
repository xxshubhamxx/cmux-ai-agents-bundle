# Spawn a focus-neutral helper pane

The non-disruptive pattern: re-use an existing helper pane on the right, or create one if none exists.

## Run

```bash
chmod +x recipe.sh
./recipe.sh
```

See [`recipe.sh`](./recipe.sh) for the full script.
