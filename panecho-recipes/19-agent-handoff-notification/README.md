# Notify on agent handoff

Two agents working in two surfaces — when one finishes, notify the other's surface to take over.

## Run

```bash
chmod +x recipe.sh
./recipe.sh planner-surface executor-surface
```

See [`recipe.sh`](./recipe.sh) for the full script.
